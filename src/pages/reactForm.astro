---
import Form from '../components/Form.jsx';
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';

const kod = `
import React, { useState } from "react";

function Form() {
  const [formData, setFormData] = useState({});

  function handleChange(event) {
    setFormData({
      ...formData,
      [event.target.name]: event.target.value
    });
  }

  function handleSubmit(event) {
    event.preventDefault();
    // Form verilerini bir API'ye göndermek için buraya kod yazabilirsiniz.
  }

  return (
    <form onSubmit={handleSubmit} className="form">
      <div className="form-group">
        <label htmlFor="name">İsim:</label>
        <input type="text" name="name" className="form-control" onChange={handleChange} />
      </div>
      <div className="form-group">
        <label htmlFor="email">E-posta:</label>
        <input type="email" name="email" className="form-control" onChange={handleChange} />
      </div>
      <div className="form-group">
        <label htmlFor="phone">Telefon:</label>
        <input type="tel" name="phone" className="form-control" onChange={handleChange} />
      </div>
      <div className="form-group">
        <label htmlFor="gender">Cinsiyet:</label>
        <select name="gender" className="form-control" onChange={handleChange}>
          <option value="">Lütfen bir seçim yapın</option>
          <option value="male">Erkek</option>
          <option value="female">Kadın</option>
          <option value="leopard">Leopard 1</option>
        </select>
      </div>
      <div className="form-group">
        <label htmlFor="birthdate">Doğum Tarihi:</label>
        <input type="date" name="birthdate" className="form-control" onChange={handleChange} />
      </div>
      <button type="submit" className="btn btn-primary">Gönder</button>
    </form>
  );
}

export default Form;
`;	
---
<Layout>
  <Header 
  head="Kayıt sayfası" 
  body="ReactJS, Facebook tarafından geliştirilen açık kaynak bir JavaScript kütüphanesidir. React, modern web uygulamaları oluşturmak için kullanılan popüler bir araçtır. React, uygulama geliştiricilerine, verimli bir şekilde bileşen tabanlı arayüzler oluşturmalarına olanak tanır. React, karmaşık uygulamaların bileşenlerini oluşturmak için kullanılan JSX adlı bir sözdizimi kullanır. JSX, HTML benzeri bir dil ile JavaScript kodlarının birleştirilmesine izin verir. React, bileşenlerin durumunu yönetmek için bir yönetim sistemi sağlar ve bileşenlerin hızlı bir şekilde yeniden kullanılmasını sağlar. React, yüksek performanslı bir arayüz oluşturmak için, bileşenlerin sadece güncellenmesi gerektiği zamanlarda yenilenmesini sağlayan sanal DOM adlı bir yöntem kullanır. React, birçok büyük şirket tarafından kullanılan popüler bir teknolojidir ve büyük bir topluluğa sahiptir. React ayrıca, diğer kütüphaneler ve araçlar ile kolayca entegre edilebilir."
  code={kod} />
  <Form client:load/>
</Layout>